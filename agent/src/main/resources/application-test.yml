app:
  device-script:
    commands:
      - type: ADBShell
        when: setUp
        matcher: '.*'
        inline: |
          test command 1
      - type: ADBShell
        when: setUp
        matcher: '.*test.*'
        inline: |
          test command 2
          test command 3
      - type: ADBShell
        when: tearDown
        matcher: 'com.microsoft.test'
        inline: |
          test command 4