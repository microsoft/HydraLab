buildscript {
    repositories {
        maven {
            url "https://plugins.gradle.org/m2/"
        }
        google()
    }
    dependencies {
        classpath 'com.microsoft.hydralab:gradle_plugin:${version}'
    }
}

apply plugin: "com.microsoft.hydralab.client-util"